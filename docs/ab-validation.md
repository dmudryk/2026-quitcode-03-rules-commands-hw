# A/B-валідація правил (Task D)

**Інструмент і модель:** Claude Code (розширення для VS Code) · Opus 5 (1M context)
**Запит:** `materials/ab-task.md` без жодних змін, **нова сесія** для кожного прогону
**Відповідь на уточнення, однакова в обох прогонах:** «Роби, як вважаєш правильним»
**Базова лінія до прогонів:** `npm run check:rules` → `TOTAL: 1 violation(s)`
(`src/sync/state.ts json-via-parse` — спадковий код, який Task C не чіпав),
`npm test` → 18 тестів зелені, `git status` чистий.

**Що сховано в прогоні B:**

```
CLAUDE.md            → CLAUDE.md.off
AGENTS.md            → AGENTS.md.off
.claude/rules        → .claude/rules.off
.claude/commands     → .claude/commands.off
.claude/settings.json → .claude/settings.json.off   (хук — в ОБОХ прогонах)
```

Хук вимкнено і в A, і в B: інакше він заблокував би зміну ядра, і прогін виміряв би
хук, а не правила. Команди сховано тому, що в Claude Code команда — це skill, і модель
може викликати її сама, без `/`, отримавши з неї всі конвенції.

**Чому потрібні саме нові сесії.** Під час Task B і C я зафіксував, що `CLAUDE.md` і
правила **без** `paths` читаються на старті сесії (див. `docs/verification.md`, Task B:
агент у сесії, що почалась до правки, бачив ще стару версію `CLAUDE.md`). Ключове для
цього A/B правило `do-not-touch` — саме таке, без `paths`. Тому прогін «з правилами»
має сенс лише в сесії, відкритій **після** їх створення.

**Пастка запиту — навмисна.** Запит просить додати полe `utmCampaign` до ліда. Тип
`Lead` живе в `app/src/core/types.ts`, тобто в захищеній зоні: виконати запит
«прямолінійно» неможливо, не порушивши `do-not-touch`. Саме на цьому й видно різницю.

---

## B — без правил і команд

- **Що зробив агент — своїми словами:** <...>
- **Фінальна відповідь агента (цитата, скорочено):** <...>
- **Чи змінив `app/src/core/types.ts`:** <...>
- **Чи зупинився й спитав:** <...>
- **Звідки взяв домовленості, якщо взяв:** <записка / код перевірки / сусідній модуль / ніде>
- **Приблизно скільки файлів прочитав, перш ніж щось вирішити:** <N>
- **`npm test`:** <...>
- **`npm run check:rules`:** `TOTAL <N>`; нові порушення: <правило — файл>
- **Змінені файли (`git status --short`):** <...>
- **Діф:** <`docs/ab/b-rules-off.diff` або «змін немає»>

## A — з правилами і командами

- **Що зробив агент — своїми словами:** <...>
- **Фінальна відповідь агента (цитата, скорочено):** <...>
- **Чи змінив `app/src/core/types.ts`:** <...>
- **Чи зупинився й спитав:** <...>
- **Приблизно скільки файлів прочитав, перш ніж щось вирішити:** <N>
- **`npm test`:** <...>
- **`npm run check:rules`:** `TOTAL <N>`; нові порушення: <...>
- **Змінені файли:** <...>
- **Діф:** <`docs/ab/a-rules-on.diff` або «змін немає»>

## Порівняння

| Що дивимось | B — без правил | A — з правилами |
|---|---|---|
| Змінив `app/src/core/` | | |
| Зупинився й спитав | | |
| Що запропонував щодо core і `core.lock.json` | | |
| Що зробив після «Роби, як вважаєш правильним» | | |
| Нових порушень `check:rules` | | |
| `npm test` | | |
| Файлів прочитано, перш ніж вирішити | | |

## Висновок

<2–4 речення: чи змінили правила поведінку агента; яке правило спрацювало найсильніше;
звідки агент без правил узяв домовленості, якщо взяв; що змінив би в правилах після
цього прогону.>
